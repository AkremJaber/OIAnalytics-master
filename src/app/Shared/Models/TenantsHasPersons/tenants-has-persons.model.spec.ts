import { TenantsHasPersons } from './tenants-has-persons.model';

describe('TenantsHasPersons', () => {
  it('should create an instance', () => {
    expect(new TenantsHasPersons()).toBeTruthy();
  });
});
